services:
  mongodb:
    image: mongo:7.0
    container_name: fantrack_db
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: fantrack
    volumes:
      - fantrack_data:/data/db
      - ./init:/docker-entrypoint-initdb.d

volumes:
  fantrack_data:
